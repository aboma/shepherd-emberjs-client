<div id="portfolios">
	<div id="portfolio-header">
		<div id="portfolio-list">
			{{view Shepherd.PortfolioSelect2
		       contentBinding="controller.savedSortedContent"
		       optionValuePath="content.id"
		       optionLabelPath="content.name"
		       selectionBinding="controller.selectedPortfolio" 
		       prompt="Please select a Portfolio"
		    }}
		</div>
		<div id="create-port-link">{{#link-to "portfolios.new"}}Create{{/link-to}}</div>
		<div id="portfolio-metadata">
			{{ outlet master }}
		</div>
	</div>
	<div id="portfolio-details">
		{{ outlet detail }}
	</div>
</div>
